# https://aka.ms/yaml
name: $(BuildDefinitionName)_$(Rev:.rr)
resources:
- repo: self
trigger: none
queue:
  name: Hosted VS2017
  demands: 
  - msbuild
  - visualstudio
steps:
- powershell: |
   write-host $(Build.BuildNumber)
   #.\go.ps1 -msbuild "C:\Program Files (x86)\Microsoft Visual Studio\2017\Enterprise\MSBuild\15.0\Bin\msbuild.exe" -packageSources $(DXApiFeed)
  displayName: 'Build'
  failOnStderr: true

