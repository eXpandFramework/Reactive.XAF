# https://aka.ms/yaml
variables:
  - group: Keys
trigger: 
  branches:
    include:
      - master
pool:
  vmImage: vs2017-win2016
steps:
- checkout: self
  clean: true
- task: PowerShell@2
  displayName: Build
  inputs:
    targetType: filePath
    filePath: .\tools\build\release-pipeline.ps1 
    arguments: $(Build.SourceBranchName) $(System.DefaultworkingDirectory) $(GitHubUserName) $(GitHubPass) $(DXApiFeed)
- powershell: | 
- task: PowerShell@2
  displayName: Publish
  inputs:
    targetType: filePath
    filePath: .\tools\build\publishNugets.ps1 
    arguments: $(Build.SourceBranchName) $(System.DefaultworkingDirectory) $(NugetApiKey) 
    ignoreLASTEXITCODE: true